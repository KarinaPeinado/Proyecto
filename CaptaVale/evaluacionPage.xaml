<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CaptaVale.evaluacionPage"
             xmlns:inputLayout="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core">

    <ScrollView>
        <StackLayout>

            <Label Text="LISTADO DE PROSPECTOS"
                   VerticalOptions="Center" 
                   HorizontalOptions="Center"
                   FontSize="22"
                   CharacterSpacing="2"
                   Margin="0,20,0,0"
                   FontAttributes="Bold"/>

            <Label Text="PENDIENTES"
                   VerticalOptions="Center" 
                   HorizontalOptions="Center"
                   FontSize="18"
                   CharacterSpacing="2"
                   Margin="0,10,0,0"
                   FontAttributes="Bold"/>

            <ListView x:Name="listViewProspEvaluacion"></ListView>

        </StackLayout>
    </ScrollView>
</ContentPage>