<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.ListView;assembly=Syncfusion.Maui.ListView"
             x:Class="CaptaVale.estatusPage">

    <ScrollView>
        <StackLayout>

            <Label Text="LISTADO DE PROSPECTOS"
                   VerticalOptions="Center" 
                   HorizontalOptions="Center"
                   FontSize="22"
                   CharacterSpacing="2"
                   Margin="0,20,0,0"
                   FontAttributes="Bold"/>

            <Label Text="PROMOTOR"
                   VerticalOptions="Center" 
                   HorizontalOptions="Center"
                   FontSize="18"
                   CharacterSpacing="2"
                   Margin="0,10,0,0"
                   FontAttributes="Bold"/>

            <Button HorizontalOptions="End"
                    VerticalOptions="End"
                    BackgroundColor="Transparent"
                    Margin="20,0,10,0"
                    ImageSource="actualizar.png"
                    WidthRequest="20"
                    HeightRequest="20"
                    Clicked="btnRefresh_Clicked"/>

            <ListView x:Name="listViewProspectos"></ListView>

        </StackLayout>
    </ScrollView>
</ContentPage>